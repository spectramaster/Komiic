# ChipQuest 关卡设计详解

## 📚 目录
1. [第一章：数字王国 (GPIO Kingdom)](#第一章数字王国)
2. [第二章：时间神殿 (Timer Temple)](#第二章时间神殿)
3. [第三章:中断深渊 (Interrupt Abyss)](#第三章中断深渊)
4. [第四章：通信迷宫 (Communication Labyrinth)](#第四章通信迷宫)
5. [第五章：模拟世界 (Analog Realm)](#第五章模拟世界)
6. [第六章：项目工坊 (Project Workshop)](#第六章项目工坊)

---

## 第一章：数字王国 (GPIO Kingdom)

### 章节主题
学习GPIO（通用输入输出）的基础知识，掌握数字信号的控制。

### 故事背景
在芯片世界的边缘，有一个由数字信号统治的王国。这里的一切只有两种状态：高电平和低电平，1和0。作为新手芯片唤醒师，你的第一个任务是学会控制这些数字引脚。

---

### 关卡1-1：觉醒之光 (The First Light)

**难度**: ⭐ 新手

**学习目标**:
- GPIO输出模式配置
- 基础的LED控制
- HAL库初始化流程

**剧情**:
```
[场景：黑暗的实验室，一颗STM32芯片静静躺在桌上]

艾达博士："欢迎来到芯片世界！你看到那颗芯片了吗？它已经沉睡了很久。
           在它的PC13引脚上，连接着一盏LED。用你的代码唤醒它吧！"

[提示框弹出]
💡 知识点：GPIO（General Purpose Input/Output）是单片机与外界交互的基础。
           每个GPIO引脚可以配置为输出模式（驱动LED、继电器等）或
           输入模式（读取按键、传感器等）。
```

**任务目标**:
1. ✅ 配置GPIOC Pin13为输出模式
2. ✅ 让LED每秒闪烁一次（频率1Hz，±10%容差）
3. ✅ 闪烁10次后保持常亮状态

**虚拟硬件配置**:
```
开发板显示：
┌─────────────────┐
│   STM32F103C8   │
│                 │
│  💡 LED (PC13)  │  ← 玩家要控制的LED
│                 │
│  ⚡ PWR (绿灯)  │  ← 电源指示（常亮）
└─────────────────┘
```

**提供的代码模板**:
```c
/**
 * 关卡 1-1: 觉醒之光
 * 目标：让LED闪烁10次，然后保持常亮
 */

#include "stm32f1xx_hal.h"

// 主函数
void Mission_1_1(void) {
    // ===== 步骤1: 使能GPIOC时钟 =====
    // 💡提示：在STM32中，使用外设前必须先使能其时钟
    __HAL_RCC_GPIOC_CLK_ENABLE();

    // ===== 步骤2: 配置GPIO =====
    GPIO_InitTypeDef GPIO_InitStruct = {0};

    GPIO_InitStruct.Pin = GPIO_PIN_13;
    GPIO_InitStruct.Mode = __________;  // TODO: 填写输出模式
                                        // 提示：使用 GPIO_MODE_OUTPUT_PP
    GPIO_InitStruct.Pull = GPIO_NOPULL;
    GPIO_InitStruct.Speed = GPIO_SPEED_FREQ_LOW;

    HAL_GPIO_Init(GPIOC, &GPIO_InitStruct);

    // ===== 步骤3: 闪烁10次 =====
    for(int i = 0; i < ____; i++) {  // TODO: 填写循环次数

        // TODO: 切换LED状态
        // 提示：使用 HAL_GPIO_TogglePin(GPIOC, GPIO_PIN_13);
        ____________________________________;

        // TODO: 延时1秒
        // 提示：HAL_Delay的参数单位是毫秒
        HAL_Delay(______);
    }

    // ===== 步骤4: 保持常亮 =====
    // TODO: 设置LED为高电平（点亮）
    // 提示：使用 HAL_GPIO_WritePin(GPIOC, GPIO_PIN_13, GPIO_PIN_SET);
    ____________________________________________________;
}
```

**提示系统** (3次免费提示):
1. **提示1**: "GPIO输出模式有几种类型：推挽输出(PP)、开漏输出(OD)。对于驱动LED，我们通常使用推挽输出：`GPIO_MODE_OUTPUT_PP`"

2. **提示2**: "循环10次很简单：`for(int i = 0; i < 10; i++)`。HAL_GPIO_TogglePin()函数会自动切换引脚状态。"

3. **提示3 (完整答案)**:
```c
GPIO_InitStruct.Mode = GPIO_MODE_OUTPUT_PP;
for(int i = 0; i < 10; i++) {
    HAL_GPIO_TogglePin(GPIOC, GPIO_PIN_13);
    HAL_Delay(1000);
}
HAL_GPIO_WritePin(GPIOC, GPIO_PIN_13, GPIO_PIN_SET);
```

**评分标准**:
- ⭐ **1星 (基础完成)**:
  - LED成功闪烁
  - 最终保持常亮
  - 奖励：+100 XP, +50晶体

- ⭐⭐ **2星 (代码优化)**:
  - 1星要求 +
  - 代码行数 ≤ 30行
  - 内存占用 ≤ 256字节
  - 奖励：+200 XP, +100晶体

- ⭐⭐⭐ **3星 (完美通关)**:
  - 2星要求 +
  - 代码行数 ≤ 20行
  - 内存占用 ≤ 128字节
  - 闪烁频率精度 ±5%
  - 通关时间 ≤ 3分钟
  - 奖励：+500 XP, +200晶体

**解锁奖励**:
- 🏆 成就：**"第一道光"** - 点亮你的第一盏LED
- 📚 代码模板：**"LED基础控制"** (可在后续关卡中复用)
- 🎓 知识卡片：**"GPIO工作原理"**
- 🔓 解锁：关卡1-2

**通关动画**:
```
[LED闪烁10次后保持常亮]
[虚拟开发板发出金色光芒]
[弹出成绩卡片]

┌──────────────────────────┐
│   🎉 任务完成！           │
│                          │
│   ⭐⭐⭐ 完美！         │
│                          │
│   📊 通关数据：           │
│   代码行数: 18           │
│   内存占用: 96 字节      │
│   用时: 2分30秒          │
│   频率精度: 99.8%        │
│                          │
│   🎁 奖励：               │
│   +500 XP                │
│   +200 晶体              │
│   解锁成就：第一道光     │
│                          │
│   [继续下一关 →]         │
└──────────────────────────┘

[艾达博士]："做得好！你已经掌握了GPIO输出的基础。
            接下来，让我们学习如何读取外部信号。"
```

---

### 关卡1-2：按键试炼 (Button Quest)

**难度**: ⭐ 新手

**学习目标**:
- GPIO输入模式配置
- 读取按键状态
- 简单的条件判断

**剧情**:
```
艾达博士："光会输出还不够，一个优秀的芯片唤醒师必须学会倾听。
           现在，我要教你读取按键的状态。按下按键时，让LED点亮；
           松开时，LED熄灭。这就是输入与输出的联动！"
```

**任务目标**:
1. ✅ 配置PA0为输入模式（上拉）
2. ✅ 配置PC13为输出模式
3. ✅ 按键按下时，LED点亮
4. ✅ 按键松开时，LED熄灭
5. ✅ 持续检测10秒

**虚拟硬件配置**:
```
开发板显示：
┌─────────────────┐
│   STM32F103C8   │
│                 │
│  💡 LED (PC13)  │  ← 根据按键状态变化
│  🔘 KEY (PA0)   │  ← 玩家可以点击
└─────────────────┘

[玩家可以用鼠标点击虚拟按键，模拟按下/松开]
```

**代码模板**:
```c
/**
 * 关卡 1-2: 按键试炼
 * 目标：按键控制LED
 */

#include "stm32f1xx_hal.h"

void Mission_1_2(void) {
    // 使能时钟
    __HAL_RCC_GPIOA_CLK_ENABLE();
    __HAL_RCC_GPIOC_CLK_ENABLE();

    GPIO_InitTypeDef GPIO_InitStruct = {0};

    // 配置LED引脚（输出）
    GPIO_InitStruct.Pin = GPIO_PIN_13;
    GPIO_InitStruct.Mode = GPIO_MODE_OUTPUT_PP;
    GPIO_InitStruct.Pull = GPIO_NOPULL;
    GPIO_InitStruct.Speed = GPIO_SPEED_FREQ_LOW;
    HAL_GPIO_Init(GPIOC, &GPIO_InitStruct);

    // TODO: 配置按键引脚（输入，上拉）
    GPIO_InitStruct.Pin = GPIO_PIN_0;
    GPIO_InitStruct.Mode = __________;  // 提示：GPIO_MODE_INPUT
    GPIO_InitStruct.Pull = __________;  // 提示：GPIO_PULLUP
    HAL_GPIO_Init(GPIOA, &GPIO_InitStruct);

    // 循环检测10秒
    uint32_t startTime = HAL_GetTick();
    while((HAL_GetTick() - startTime) < 10000) {

        // TODO: 读取按键状态
        GPIO_PinState keyState = HAL_GPIO_ReadPin(______, ______);

        // TODO: 根据按键状态控制LED
        if(keyState == GPIO_PIN_RESET) {  // 按键按下（低电平）
            // 点亮LED
            ________________________________________;
        } else {
            // 熄灭LED
            ________________________________________;
        }

        HAL_Delay(10);  // 小延时，防止CPU占用过高
    }
}
```

**星级评分**:
- ⭐ 1星：功能实现
- ⭐⭐ 2星：添加按键消抖（防止抖动）
- ⭐⭐⭐ 3星：用状态机实现，响应时间<20ms

**解锁奖励**:
- 🏆 成就：**"倾听者"** - 读取你的第一个输入信号
- 📚 代码模板：**"按键消抖算法"**
- 🔓 解锁：关卡1-3

---

### 关卡1-3：流水灯阵 (LED Array)

**难度**: ⭐⭐ 简单

**学习目标**:
- 控制多个GPIO引脚
- 循环与数组的应用
- 位操作

**任务目标**:
1. ✅ 控制8个LED (PA0-PA7)
2. ✅ 实现流水灯效果（从左到右）
3. ✅ 每个LED点亮200ms
4. ✅ 循环3次后停止

**虚拟硬件**:
```
┌─────────────────────────────┐
│       STM32F103C8           │
│                             │
│  💡💡💡💡💡💡💡💡        │
│  PA0 1 2 3 4 5 6 7          │
│                             │
│  [动画：LED从左到右依次亮起] │
└─────────────────────────────┘
```

**代码挑战**:
```c
// 方法1：逐个控制（简单但代码冗长）
HAL_GPIO_WritePin(GPIOA, GPIO_PIN_0, GPIO_PIN_SET);
HAL_Delay(200);
HAL_GPIO_WritePin(GPIOA, GPIO_PIN_0, GPIO_PIN_RESET);
HAL_GPIO_WritePin(GPIOA, GPIO_PIN_1, GPIO_PIN_SET);
// ... 重复8次

// 方法2：循环控制（推荐）
for(int i = 0; i < 8; i++) {
    HAL_GPIO_WritePin(GPIOA, 1 << i, GPIO_PIN_SET);
    HAL_Delay(200);
    HAL_GPIO_WritePin(GPIOA, 1 << i, GPIO_PIN_RESET);
}

// 方法3：直接写寄存器（大师级）
GPIOA->ODR = 0x01;
for(int i = 0; i < 8; i++) {
    GPIOA->ODR <<= 1;
    HAL_Delay(200);
}
```

**三星要求**:
- 使用位操作优化代码
- 代码行数 ≤ 15行
- 无硬编码，使用循环

---

### 关卡1-4：蜂鸣警报 (Buzzer Alarm)

**难度**: ⭐⭐ 简单

**学习目标**:
- 有源蜂鸣器控制
- PWM预备知识（频率的概念）

**任务目标**:
1. ✅ 控制蜂鸣器发出声音
2. ✅ 实现"嘀-嘀-嘀"的报警模式
3. ✅ 短响3次（200ms on, 200ms off）
4. ✅ 长响1次（1000ms）

**虚拟硬件**:
```
┌─────────────────┐
│  STM32F103C8    │
│                 │
│  🔊 Buzzer (PB0)│  ← 发出声音（有动画和音效）
└─────────────────┘

[点击运行后，用户能听到"嘀-嘀-嘀-哔————"的声音]
```

---

### 关卡1-5 (Boss关)：交通灯系统 (Traffic Light)

**难度**: ⭐⭐⭐ 中等

**学习目标**:
- 综合应用GPIO
- 状态机编程思想
- 定时控制

**剧情**:
```
艾达博士："最后的考验来了！我需要你设计一个交通灯控制系统。
           这将考验你对GPIO的综合运用能力。准备好了吗？"

[场景切换到十字路口]
```

**任务目标**:
1. ✅ 控制6个LED（南北：红黄绿，东西：红黄绿）
2. ✅ 实现标准交通灯逻辑：
   - 南北绿灯 30秒，东西红灯
   - 南北黄灯 3秒，东西红灯
   - 南北红灯，东西绿灯 30秒
   - 南北红灯，东西黄灯 3秒
   - 循环
3. ✅ 运行2个完整周期（约2分钟）

**虚拟硬件**:
```
┌──────────────────────────────┐
│     十字路口俯视图             │
│                              │
│         🔴 🟡 🟢             │
│            ↑                 │
│            |  南北方向        │
│   🔴🟡🟢 ← + → 🔴🟡🟢      │
│            |  东西方向        │
│            ↓                 │
│         🔴 🟡 🟢             │
│                              │
│   [实时动画显示灯光变化]      │
└──────────────────────────────┘
```

**代码框架**:
```c
typedef enum {
    STATE_NS_GREEN,   // 南北绿灯
    STATE_NS_YELLOW,  // 南北黄灯
    STATE_EW_GREEN,   // 东西绿灯
    STATE_EW_YELLOW   // 东西黄灯
} TrafficState;

void Mission_1_5(void) {
    // TODO: 初始化6个GPIO

    TrafficState state = STATE_NS_GREEN;
    uint32_t stateStartTime = HAL_GetTick();

    for(int cycle = 0; cycle < 2; cycle++) {
        while(/* 一个周期 */) {
            switch(state) {
                case STATE_NS_GREEN:
                    // TODO: 设置灯光状态
                    if(HAL_GetTick() - stateStartTime > 30000) {
                        state = STATE_NS_YELLOW;
                        stateStartTime = HAL_GetTick();
                    }
                    break;

                // TODO: 实现其他状态
            }
            HAL_Delay(100);
        }
    }
}
```

**星级评分**:
- ⭐ 1星：正确实现逻辑
- ⭐⭐ 2星：使用状态机，代码结构清晰
- ⭐⭐⭐ 3星：添加行人按钮（紧急切换到红灯）

**通关奖励**:
- 🏆 成就：**"交通指挥大师"**
- 🎖️ 徽章：**GPIO王者（金）**
- 📜 解锁：第二章 - 时间神殿
- 💎 特殊奖励：**"状态机设计模式"** 代码库

**章节结束动画**:
```
[交通灯顺利运行]
[画面拉远，显示整个城市的交通系统井然有序]

艾达博士："太棒了！你已经完全掌握了GPIO的精髓。
           数字王国的大门为你敞开。但这只是开始...
           接下来，我将带你进入时间的领域——定时器的世界！"

[屏幕淡出，显示第二章预告]

┌─────────────────────────────┐
│  🎬 下一章预告：时间神殿     │
│                             │
│  "在这里,时间不再流逝,       │
│   而是被你精确控制..."       │
│                             │
│  学习内容:                   │
│  ⏱️ 定时器配置               │
│  📊 PWM波形生成              │
│  🎵 音乐播放                 │
│  🤖 舵机控制                 │
│                             │
│  [解锁第二章 →]             │
└─────────────────────────────┘
```

---

## 第二章：时间神殿 (Timer Temple)

### 章节主题
掌握定时器（Timer）的使用，学习时间的精确控制和PWM波形生成。

### 故事背景
```
在芯片世界的中心，矗立着一座神秘的神殿。
这里的时间由晶振的脉搏驱动，每一次滴答都精确到微秒。
作为芯片唤醒师，你必须学会掌控时间的力量。
```

---

### 关卡2-1：时钟迷宫 (Clock Configuration)

**难度**: ⭐⭐ 简单

**学习目标**:
- 系统时钟配置
- 时钟树的概念
- 预分频器的作用

**剧情**:
```
神殿守护者："欢迎来到时间神殿。在使用定时器之前，
             你必须先理解时钟系统。看，这是STM32的时钟树..."

[展示时钟树图]
```

**任务目标**:
1. ✅ 配置系统时钟为72MHz
2. ✅ 配置TIM2时钟
3. ✅ 理解预分频器的计算

**互动教学**:
```
[可视化时钟树]

     HSE (8MHz)
        ↓
     ×9 PLL
        ↓
   SYSCLK (72MHz)
        ↓
    AHB (72MHz)
        ↓
   APB1 (36MHz) → TIM2
        ↓
   APB2 (72MHz)

[拖动滑块调整分频系数，实时看到频率变化]
```

**知识检测**:
```
问题：要让TIM2每1ms触发一次中断，预分频器应设置为？
A. 71
B. 35
C. 3599
D. 7199

[选择正确答案后才能继续]
```

---

### 关卡2-2：节拍大师 (Timer Basics)

**难度**: ⭐⭐ 简单

**学习目标**:
- 基础定时器配置
- 定时器中断
- 计数器原理

**任务目标**:
1. ✅ 配置TIM2产生1秒定时
2. ✅ 在定时器中断中翻转LED
3. ✅ 实现精确的1Hz闪烁

**代码模板**:
```c
// 定时器配置
TIM_HandleTypeDef htim2;

void TIM2_Init(void) {
    __HAL_RCC_TIM2_CLK_ENABLE();

    htim2.Instance = TIM2;
    htim2.Init.Prescaler = ______;  // TODO: 计算预分频值
    htim2.Init.Period = ______;     // TODO: 计算重装载值
    htim2.Init.CounterMode = TIM_COUNTERMODE_UP;
    htim2.Init.ClockDivision = TIM_CLOCKDIVISION_DIV1;

    HAL_TIM_Base_Init(&htim2);
    HAL_TIM_Base_Start_IT(&htim2);  // 启动中断模式

    // TODO: 使能中断
    HAL_NVIC_SetPriority(TIM2_IRQn, 0, 0);
    HAL_NVIC_EnableIRQ(TIM2_IRQn);
}

// 中断处理函数
void TIM2_IRQHandler(void) {
    HAL_TIM_IRQHandler(&htim2);
}

void HAL_TIM_PeriodElapsedCallback(TIM_HandleTypeDef *htim) {
    if(htim->Instance == TIM2) {
        // TODO: 翻转LED
    }
}
```

**可视化**:
```
[虚拟示波器显示]
     ┌────────────────────────────┐
     │  定时器计数器值 (CNT)       │
     │                            │
ARR  ├─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─│
     │         ╱|        ╱|      │
     │       ╱  |      ╱  |      │
     │     ╱    |    ╱    |      │
  0  ├───╱──────┴──╱──────┴─────│
     └────────────────────────────┘
        1s       1s
     [每到ARR值，触发中断，计数器归零]
```

---

### 关卡2-3：脉冲之舞 (PWM Generation)

**难度**: ⭐⭐⭐ 中等

**学习目标**:
- PWM原理
- 占空比控制
- 通道配置

**任务目标**:
1. ✅ 生成1kHz的PWM信号
2. ✅ 占空比50%
3. ✅ 通过示波器验证波形

**虚拟硬件**:
```
┌────────────────────────────────┐
│  📊 虚拟示波器                  │
│                                │
│   ┌─┐  ┌─┐  ┌─┐  ┌─┐          │
│   │ │  │ │  │ │  │ │          │
│ ──┘ └──┘ └──┘ └──┘ └──        │
│   |←500μs→|                    │
│                                │
│  频率: 1.00 kHz ✓              │
│  占空比: 50.2% ✓               │
│  高电平: 3.29V ✓               │
└────────────────────────────────┘
```

**代码模板**:
```c
void PWM_Init(void) {
    TIM_OC_InitTypeDef sConfigOC = {0};

    htim3.Instance = TIM3;
    htim3.Init.Prescaler = ______;  // TODO
    htim3.Init.Period = ______;     // TODO
    HAL_TIM_PWM_Init(&htim3);

    sConfigOC.OCMode = TIM_OCMODE_PWM1;
    sConfigOC.Pulse = ______;  // TODO: 计算占空比50%的值
    sConfigOC.OCPolarity = TIM_OCPOLARITY_HIGH;

    HAL_TIM_PWM_ConfigChannel(&htim3, &sConfigOC, TIM_CHANNEL_1);
    HAL_TIM_PWM_Start(&htim3, TIM_CHANNEL_1);
}
```

---

### 关卡2-4：呼吸灯魔法 (Breathing LED)

**难度**: ⭐⭐⭐ 中等

**学习目标**:
- 动态调整PWM占空比
- 实现平滑渐变效果

**任务目标**:
1. ✅ LED亮度从0%渐变到100%
2. ✅ 再从100%渐变到0%
3. ✅ 周期2秒，平滑过渡

**效果**:
```
[LED亮度变化曲线]
100% ┤     ╱╲     ╱╲
     │    ╱  ╲   ╱  ╲
 50% ┤   ╱    ╲ ╱    ╲
     │  ╱      ╲      ╲
  0% ┼─╱────────╲──────╲──
     └─────────────────────→ 时间
      0s  1s  2s  3s  4s

[实际LED发出柔和的呼吸效果]
```

**代码模板**:
```c
void BreathingLED(void) {
    PWM_Init();

    while(1) {
        // 渐亮
        for(int duty = 0; duty <= 100; duty++) {
            // TODO: 设置占空比
            __HAL_TIM_SET_COMPARE(&htim3, TIM_CHANNEL_1, duty);
            HAL_Delay(10);
        }

        // 渐暗
        for(int duty = 100; duty >= 0; duty--) {
            __HAL_TIM_SET_COMPARE(&htim3, TIM_CHANNEL_1, duty);
            HAL_Delay(10);
        }
    }
}
```

**三星挑战**:
- 使用三角波函数实现更平滑的效果
- 同时控制3个LED，形成彩虹呼吸效果

---

### 关卡2-5 (Boss关)：音乐盒 (Music Box)

**难度**: ⭐⭐⭐⭐ 困难

**学习目标**:
- PWM产生不同频率的声音
- 音符与频率的对应关系
- 定时器精确控制

**任务目标**:
1. ✅ 使用无源蜂鸣器播放《小星星》
2. ✅ 音准误差 < 5%
3. ✅ 节奏准确

**乐谱**:
```
音符:  C   C   G   G  A   A   G
频率: 262 262 392 392 440 440 392 (Hz)
时长:  1   1   1   1  1   1   2  (拍)

音符:  F   F   E   E  D   D   C
频率: 349 349 330 330 294 294 262 (Hz)
时长:  1   1   1   1  1   1   2  (拍)
```

**代码框架**:
```c
// 音符频率表
typedef struct {
    char note;      // 音符名
    uint16_t freq;  // 频率(Hz)
} Note;

const Note NOTES[] = {
    {'C', 262}, {'D', 294}, {'E', 330},
    {'F', 349}, {'G', 392}, {'A', 440}, {'B', 494}
};

void playNote(char note, uint16_t duration) {
    // TODO: 根据频率计算ARR值
    uint16_t arr = 72000000 / (Prescaler * freq) - 1;

    // TODO: 设置定时器
    __HAL_TIM_SET_AUTORELOAD(&htim3, arr);
    __HAL_TIM_SET_COMPARE(&htim3, TIM_CHANNEL_1, arr / 2);

    HAL_TIM_PWM_Start(&htim3, TIM_CHANNEL_1);
    HAL_Delay(duration);
    HAL_TIM_PWM_Stop(&htim3, TIM_CHANNEL_1);
}

void Mission_2_5(void) {
    PWM_Init();

    // 小星星乐谱
    char melody[] = "CCGGAAG FFEEDDC";
    uint16_t durations[] = {400,400,400,400,400,400,800,
                           400,400,400,400,400,400,800};

    for(int i = 0; i < sizeof(melody); i++) {
        if(melody[i] != ' ') {
            playNote(melody[i], durations[i]);
            HAL_Delay(50);  // 音符间隔
        }
    }
}
```

**虚拟硬件**:
```
┌────────────────────────────────┐
│  🎵 Music Visualizer           │
│                                │
│  ♪  ♫  ♪  ♫  ♪  ♫  ♪          │
│  [频谱动画]                     │
│   ▂▄▆█▆▄▂                      │
│                                │
│  当前音符: G (392Hz)            │
│  准确度: 98.5% ✓               │
│                                │
│  🔊 [蜂鸣器发出声音]            │
└────────────────────────────────┘
```

**通关奖励**:
- 🏆 成就：**"音乐家"**
- 🎖️ 徽章：**定时器大师（金）**
- 📜 解锁：第三章 - 中断深渊
- 💎 特殊：**"音符库"**（包含所有音符频率）

---

## 第三章到第五章设计概要

### 第三章：中断深渊
- 3-1: 外部中断（按键中断）
- 3-2: 中断优先级管理
- 3-3: 定时器中断深度应用
- 3-4: 中断嵌套与临界区
- Boss: 实时多按键系统

### 第四章：通信迷宫
- 4-1: USART串口收发
- 4-2: 串口协议解析
- 4-3: I2C读写EEPROM
- 4-4: SPI驱动OLED屏幕
- Boss: 传感器网络

### 第五章：模拟世界
- 5-1: ADC单通道采集
- 5-2: ADC多通道扫描
- 5-3: DAC波形生成
- 5-4: DMA高速传输
- Boss: 数据采集系统

---

## 第六章：项目工坊 (综合项目)

### 6-1: 智能小车控制系统
**综合技能**: GPIO + Timer + PWM + USART

**功能要求**:
- 电机PWM调速
- 超声波测距（GPIO + 定时器）
- 蓝牙遥控（USART）
- 避障算法

### 6-2: 环境监测站
**综合技能**: ADC + I2C + USART + 定时器

**功能要求**:
- DHT11温湿度读取（I2C）
- 光照强度检测（ADC）
- 数据定时上传（USART）
- OLED实时显示

### 6-3: 简易示波器
**综合技能**: ADC + DMA + USART + 显示

**功能要求**:
- 高速ADC采样（DMA）
- 波形缓存与显示
- 触发模式
- PC端上位机通信

---

## 关卡设计原则总结

### 1. 渐进式难度
- 每关引入1-2个新概念
- 复用前面学到的知识
- 难度曲线平滑上升

### 2. 即时反馈
- 虚拟硬件实时响应
- 错误提示明确具体
- 成功时视觉/听觉奖励

### 3. 多样化挑战
- 不同类型的任务（填空、自由编程、优化）
- 可选的进阶挑战（三星标准）
- Boss关综合考验

### 4. 故事性包装
- 每章有连贯的故事线
- NPC引导和鼓励
- 沉浸式的世界观

### 5. 成就驱动
- 每关都有明确奖励
- 收集要素（徽章、代码库）
- 进度可视化

---

**完成这套关卡体系，学习者将掌握**:
✅ STM32 HAL库的使用
✅ GPIO、定时器、中断、通信协议
✅ 嵌入式编程思维
✅ 实际项目开发能力

**预计学习时长**: 40-60小时（取决于个人基础）
